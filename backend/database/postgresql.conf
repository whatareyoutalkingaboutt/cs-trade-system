# 2C2G 主机下的 PostgreSQL 防爆内存配置
shared_preload_libraries = 'timescaledb'
shared_buffers = 256MB
work_mem = 4MB
maintenance_work_mem = 64MB

# 预留给系统与应用进程，避免数据库抢占全部内存
effective_cache_size = 768MB
temp_buffers = 8MB
listen_addresses = '*'
port = 5432
